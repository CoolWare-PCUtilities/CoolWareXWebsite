{
  "name": "coolwarex-website",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test tests/*.test.js",
    "lint": "eslint script.js netlify/functions tests",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify:publish": "node scripts/verify-publish.mjs",
    "doctor": "node scripts/doctor.mjs",
    "deploy:preview": "node scripts/deploy-preview.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "sshpk": "^1.18.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "netlify-cli": "file:vendor/netlify-cli",
    "eslint": "file:vendor/eslint",
    "prettier": "file:vendor/prettier"
  }
}
